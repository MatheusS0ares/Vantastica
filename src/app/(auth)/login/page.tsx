import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function LoginPage() {
  return (
    <PagePlaceholder
      title="Entrar"
      description="Login via Supabase Auth — a implementar."
    />
  );
}
